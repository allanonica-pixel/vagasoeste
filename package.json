{
  "name": "vagasoeste",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:site": "npm -w apps/site run dev",
    "dev:platform": "npm -w apps/platform run dev",
    "build:site": "npm -w apps/site run build",
    "build:platform": "npm -w apps/platform run build",
    "build": "npm run build:site && npm run build:platform",
    "lint": "npm -w apps/site run lint && npm -w apps/platform run lint",
    "type-check": "npm -w apps/site run type-check && npm -w apps/platform run type-check"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "pg": "^8.20.0"
  }
}
